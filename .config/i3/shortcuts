# application launcher.
bindsym $mod+d exec "rofi -modi drun,run -show drun"

# terminal stuff
bindsym $mod+Return exec "kitty"
bindsym $mod+Shift+Return exec "kitty --config /home/snsvrno/.config/kitty/kitty-secondary.conf"

#system shutdown
bindsym $mod+Shift+Ctrl+Alt+s "shutdown -P 0"
