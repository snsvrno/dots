(defwidget time []
	(box :class "time"
		time
	)
)

(defpoll time :interval "10s"
	"date +'%H:%M'")

(defwidget date []
	(box :class "date"
		date
	)
)

(defpoll date :interval "10s"
	"date +'%m/%d'")
