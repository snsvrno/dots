# STARTUP APPS
#
# window composer
exec --no-startup-id "picom -b --experimental-backends"

# widget manager / bar
exec --no-startup-id "eww daemon"

# sets the wallpaper
exec "feh ~/.backgrounds/patterns/ --randomize --bg-tile"
