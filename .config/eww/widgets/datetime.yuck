(defwidget time []
	(box :class "widget time"
		time
	)
)

(defpoll time :interval "10s"
	"date +'%H:%M'")

(defwidget date []
	(box :class "widget date"
		date
	)
)

(defpoll date :interval "10s"
	"date +'%m/%d'")
